/***************************************************************

point(x, y, *color)

在画布上画出一个点：

* 点的位置为（x,y）
* 颜色为为color，可选参数，默认为 "orange"


***************************************************************/
canvas.showAxis();

point(100, 100);

point(200, 100, 'green');

point(300, 100, 'red');